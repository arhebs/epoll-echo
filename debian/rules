#!/usr/bin/make -f
# debian/rules — minimal dh wrapper invoking the project Makefile.

export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@

override_dh_auto_build:
	$(MAKE) ENABLE_SYSTEMD=1

override_dh_auto_clean:
	$(MAKE) clean
